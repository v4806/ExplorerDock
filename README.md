# ExplorerDock

把 Windows 11 任务栏上的文件夹窗口按钮，搬进一条常驻屏幕顶部的悬浮栏。

开着十个文件夹干活时，任务栏会被一排长得一样的文件夹按钮塞满。ExplorerDock 把这些按钮从任务栏拿走，
收进一条轻量悬浮栏，点一下就切过去 —— 任务栏留给真正需要常驻的应用。

![开启前后对比](docs/compare.png)

![悬浮栏](docs/dock.png)

演示视频：https://www.bilibili.com/video/BV1UGew6rEWA/

## 功能

- 文件夹窗口的按钮从任务栏移除，改由悬浮栏集中显示与切换；新开的自动出现，关掉的自动消失
- 左键切换、已经在最前则最小化；当前活动的文件夹会高亮
- 按钮显示文件夹名与所在路径，图标取自实际路径
- 深色 / 浅色 / 跟随系统 / 完全自定义（配色、边框线宽、整体缩放、字体）
- 悬浮栏位置可拖动并记住

## 安装

到 [Releases](../../releases) 下载 `ExplorerDock-Setup-x.y.z.exe` 双击安装（自带 .NET 运行时，无需额外环境）。
也可以下 `ExplorerDock-x.y.z-portable.zip` 免安装运行，但那需要系统已装 .NET 10 Desktop Runtime。

## 用法

| 操作 | 行为 |
| --- | --- |
| 左键点按钮 | 切到该文件夹；已在最前则最小化 |
| 中键点按钮 | 关闭该文件夹窗口 |
| 右键点按钮 | 切换 / 最小化 / 复制路径 / 关闭窗口 等 |
| 按住悬浮栏任意位置拖动 | 移动位置（会被记住） |
| 双击 `⠿` 把手 | 隐藏悬浮栏 |
| 右键 `⠿` 把手 | 设置菜单 |
| 滚轮 | 文件夹太多时横向滚动 |

托盘图标：双击显示 / 隐藏悬浮栏，右键是全部设置。

## 已知取舍

摘除任务栏按钮用的是系统接口 `ITaskbarList::DeleteTab`。在 Windows 11 上实测，摘掉按钮的同时
这些窗口也会从 ALT+TAB 列表里消失（悬浮栏本身仍在列表中）。这是当前方案明确的代价。

## 要求

Windows 10 1809+ / Windows 11，64 位。

## 许可

[MIT](LICENSE)
