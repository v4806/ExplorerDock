<Application x:Class="ExplorerDock.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ExplorerDock"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime">
    <Application.Resources>

        <!-- ============ 自定义主题设置面板的深色控件样式 ============ -->
        <!-- 放在这里而不是运行时解析字符串：编译期就能查出模板语法问题，不会再闪退 -->
        <!-- 下面的颜色全部走 DynamicResource，由 App.ApplyMenuTheme() 按 ThemePalette 覆盖，
             这样主题编辑器自身的输入框/按钮/滑块也跟着主题变，而不是写死一套深色。 -->

        <SolidColorBrush x:Key="EditorSurface" Color="#FF2A2A30" />
        <SolidColorBrush x:Key="EditorBorder" Color="#FF3A3A42" />
        <SolidColorBrush x:Key="EditorText" Color="#FFF0F0F0" />
        <SolidColorBrush x:Key="EditorMuted" Color="#FFB0B0B8" />
        <SolidColorBrush x:Key="EditorAccent" Color="#FF4C8DF6" />
        <SolidColorBrush x:Key="EditorAccentHover" Color="#FF659BF8" />
        <SolidColorBrush x:Key="EditorAccentPressed" Color="#FF3A7BE0" />
        <SolidColorBrush x:Key="EditorHover" Color="#FF35353D" />
        <SolidColorBrush x:Key="EditorPressed" Color="#FF202025" />
        <sys:Double x:Key="EditorFontSize">12</sys:Double>

        <Style x:Key="EditorTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{DynamicResource EditorText}" />
            <Setter Property="CaretBrush" Value="{DynamicResource EditorText}" />
            <Setter Property="FontSize" Value="{DynamicResource EditorFontSize}" />
            <Setter Property="Padding" Value="8,5,8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="Bd" CornerRadius="6" Background="{DynamicResource EditorSurface}" BorderBrush="{DynamicResource EditorBorder}" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource EditorAccent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="{DynamicResource EditorFontSize}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" CornerRadius="6" Background="{DynamicResource EditorSurface}" BorderBrush="{DynamicResource EditorBorder}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorPressed}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorButtonAccent" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="{DynamicResource EditorFontSize}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" CornerRadius="6" Background="{DynamicResource EditorAccent}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorAccentHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorAccentPressed}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorSlider" TargetType="{x:Type Slider}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid VerticalAlignment="Center" Height="22">
                            <Border Height="4" CornerRadius="2" Background="{DynamicResource EditorBorder}" VerticalAlignment="Center" />
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" Focusable="False" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14" Cursor="Hand">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border CornerRadius="7" Background="{DynamicResource EditorAccent}" BorderBrush="White" BorderThickness="1" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorComboBox" TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{DynamicResource EditorText}" />
            <Setter Property="FontSize" Value="{DynamicResource EditorFontSize}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="Bd" CornerRadius="6" Background="{DynamicResource EditorSurface}" BorderBrush="{DynamicResource EditorBorder}" BorderThickness="1">
                                            <Path x:Name="Arrow" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"
                                                  Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource EditorMuted}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorHover}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                              Margin="10,0,26,0"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide"
                                   Focusable="False">
                                <Border CornerRadius="6"
                                        Margin="0,4,0,0"
                                        Background="{DynamicResource EditorSurface}"
                                        BorderBrush="{DynamicResource EditorBorder}"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource EditorAccent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorComboBoxItem" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{DynamicResource EditorText}" />
            <Setter Property="Padding" Value="8,5,8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Bd" CornerRadius="4" Background="Transparent" Padding="{TemplateBinding Padding}" Margin="2,1,2,1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource EditorBorder}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 菜单配色：与悬浮栏同一套深色玻璃风格 -->
        <SolidColorBrush x:Key="DockMenuBackground" Color="#F21B1B1F" />
        <SolidColorBrush x:Key="DockMenuBorder" Color="#33FFFFFF" />
        <SolidColorBrush x:Key="DockMenuForeground" Color="#F2F2F2" />
        <SolidColorBrush x:Key="DockMenuHighlight" Color="#26FFFFFF" />
        <SolidColorBrush x:Key="DockMenuHighlightText" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="DockMenuDisabled" Color="#5CFFFFFF" />
        <SolidColorBrush x:Key="DockMenuSeparator" Color="#1FFFFFFF" />

        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Foreground" Value="{DynamicResource DockMenuForeground}" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="MinWidth" Value="196" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Margin="10"
                                Padding="6"
                                CornerRadius="10"
                                Background="{DynamicResource DockMenuBackground}"
                                BorderBrush="{DynamicResource DockMenuBorder}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18" ShadowDepth="3" Direction="270"
                                                  Opacity="0.55" Color="Black" RenderingBias="Performance" />
                            </Border.Effect>
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="{DynamicResource DockMenuForeground}" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Bd"
                                Padding="3,7,16,7"
                                CornerRadius="7"
                                Background="Transparent"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="CheckMark"
                                           Grid.Column="0"
                                           Text="&#x2713;"
                                           FontSize="11"
                                           Margin="0,0,0,0"
                                           Visibility="Collapsed"
                                           Foreground="{DynamicResource DockMenuForeground}"
                                           VerticalAlignment="Center" />
                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center" />
                                <!-- 有子菜单时右侧那个小箭头 -->
                                <TextBlock x:Name="SubmenuArrow"
                                           Grid.Column="2"
                                           Text="&#x203A;"
                                           FontSize="12"
                                           Margin="14,0,0,0"
                                           Visibility="Collapsed"
                                           Foreground="{DynamicResource DockMenuDisabled}"
                                           VerticalAlignment="Center" />

                                <!-- 子菜单：菜单项自己负责把子项弹出来（分组菜单就靠它） -->
                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       Placement="Right"
                                       HorizontalOffset="0"
                                       VerticalOffset="-10"
                                       PopupAnimation="None">
                                    <Border Margin="8"
                                            Padding="6"
                                            CornerRadius="10"
                                            Background="{DynamicResource DockMenuBackground}"
                                            BorderBrush="{DynamicResource DockMenuBorder}"
                                            BorderThickness="1">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Direction="270"
                                                              Opacity="0.55" Color="Black" RenderingBias="Performance" />
                                        </Border.Effect>
                                        <ScrollViewer MaxHeight="640"
                                                      VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <StackPanel IsItemsHost="True"
                                                        MinWidth="176"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DockMenuHighlight}" />
                                <!-- 悬浮时文字按高亮色明暗自动反色（浅色主题下高亮是深灰块） -->
                                <Setter Property="Foreground" Value="{DynamicResource DockMenuHighlightText}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DockMenuDisabled}" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Foreground" Value="{DynamicResource DockMenuForeground}" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="{DynamicResource EditorFontSize}" />
            <Setter Property="HasDropShadow" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Margin="8"
                                Padding="11,8"
                                CornerRadius="8"
                                Background="{DynamicResource DockMenuBackground}"
                                BorderBrush="{DynamicResource DockMenuBorder}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="14" ShadowDepth="2" Direction="270"
                                                  Opacity="0.5" Color="Black" RenderingBias="Performance" />
                            </Border.Effect>
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Separator}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1" Margin="6,5" Background="{DynamicResource DockMenuSeparator}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
